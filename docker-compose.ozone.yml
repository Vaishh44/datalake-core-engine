version: "3.8"
services:
  ozone-scm:
    image: apache/ozone:1.3.0
    container_name: ozone-scm
    command: [ "ozone", "scm" ]
    ports:
      - "9876:9876"
    environment:
      OZONE_OM_SERVICE_IDS: ozone1
    networks: [ ozone-net ]

  ozone-om:
    image: apache/ozone:1.3.0
    container_name: ozone-om
    command: [ "ozone", "om" ]
    ports:
      - "9878:9878"
    environment:
      OZONE_OM_SERVICE_IDS: ozone1
      OZONE_OM_NODE_ID: om1
      OZONE_OM_ADDRESS: ozone-om
    depends_on: [ ozone-scm ]
    networks: [ ozone-net ]

  ozone-datanode:
    image: apache/ozone:1.3.0
    container_name: ozone-datanode
    command: [ "ozone", "datanode" ]
    environment:
      OZONE_SCM_IP: ozone-scm
    depends_on: [ ozone-scm ]
    networks: [ ozone-net ]

  ozone-s3g:
    image: apache/ozone:1.3.0
    container_name: ozone-s3g
    command: [ "ozone", "s3g" ]
    ports:
      - "9862:9862"
    environment:
      OZONE_OM_SERVICE_IDS: ozone1
      OZONE_OM_ADDRESS: ozone-om
    depends_on: [ ozone-om ]
    networks: [ ozone-net ]

networks:
  ozone-net:
    driver: bridge
